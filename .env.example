# VibeVoice Configuration Example
# Copy this file to .env and customize as needed

# Voice control keys
VOICEKEY=cmd_r                    # Dictation key (⌘ droite)
VOICEKEY_CMD=f12                  # AI command key (F12)
VOICEKEY_SCREENSHOT=alt_r         # AI with screenshot key (Option droite)

# AI Configuration
OLLAMA_MODEL=gemma3:27b           # Ollama model to use
INCLUDE_SCREENSHOT=false          # Enable screenshots for F12 commands (not needed for alt_r)

# Screenshot Configuration
SCREENSHOT_MAX_WIDTH=1024         # Max screenshot width in pixels (100-4096)

# Performance Tips:
# For faster screenshot processing, try smaller/faster models:
# OLLAMA_MODEL=llama3.2:3b        # Faster, smaller model
# OLLAMA_MODEL=phi3.5:3.8b        # Good balance of speed/quality
# SCREENSHOT_MAX_WIDTH=512        # Smaller images = faster processing

# Example alternative keys (uncomment to use):
# VOICEKEY=cmd_l                  # Left Command
# VOICEKEY_CMD=f11                # F11
# VOICEKEY_SCREENSHOT=alt_l       # Left Option